{
    "type": "object",
    "$id": "schema:expense:create:params",
    "required": ["parentId"],
    "additionalProperties": false,
    "properties": {
        "parentId": {
            "type": "string"
        }
    }
}
